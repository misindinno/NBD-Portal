# Active client config — generated by deploy.ps1, never commit directly
src/server/ClientConfig.js

# Root .clasp.json — swapped per client by deploy.ps1 (leading / = root only)
/.clasp.json

node_modules/
*.env

# Claude Code internal settings
.claude/

# OS
.DS_Store
Thumbs.db
